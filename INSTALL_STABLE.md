# Fresh install

Upload `DIRTLINE_MX_Arcade_3_2_3_STABLE_FreshInstall.zip` to the repository root in Codespaces and use the install commands supplied with the build.

Before committing, run:

```bash
python3 tools/full_audit.py
```

Only commit/push if it ends with `PASS`.
